# 手順書 A-02：データ層の物理基盤セットアップ（所要1時間）

## 目的
「1つの置き場」の物理実体を作る。
原本＝Google Drive `/Archive/`、索引カード＝Vault `Cards/`、の2つの箱と、
Macのcronが原本を**ローカルファイルとして**読める状態の確立。

## 手順

### ① 原本ストアの箱を作る（5分）
Google Driveのマイドライブ直下に作成：
```
/Archive/2026-08/
```
命名規則（今後の全レーン共通）：`YYYY-MM` の月フォルダ、
ファイル名は `MMDD_HHmm_内容.拡張子`（例：`0801_0812_通勤メモ.m4a`）

### ② Drive for Desktop をミラーリングに（15分）
1. **先に容量確認**：Drive のWeb画面で「保存容量」を見る。
   マイドライブ全体が数百GBある場合はミラーリングでMacのディスクを
   同量食うため、③の代替案に切り替えること
2. メニューバーのDriveアイコン → 設定（歯車）→「設定」
   →「Google Drive」タブ →「ファイルをミラーリングする」を選択
3. 同期完了を待つ（初回はサイズ次第で時間がかかる）

### ③ 代替案（マイドライブが既に大きい場合のみ）
「ファイルをストリーミングする」のまま、Finderで `Archive` フォルダを
右クリック →「オフラインアクセスを許可」。
→ Archiveだけ実体がローカルに固定され、他はクラウドのまま。
※この方式でもA-02のDoDは満たせる。ミラーリング全面適用は必須ではない

### ④ 索引カードの箱を作る（2分）
Obsidianで Vault（JohnSecondBrain）直下に `Cards/` フォルダを作成。
テストノートを1枚作る：`Cards/test.md`（A-04の動作確認にも使える）

### ⑤ 貫通テスト＝このタスクのDoD（15分）
1. **iPhoneのDriveアプリ**から適当な音声かスクショを `/Archive/2026-08/` にアップロード
2. Macのターミナルで実体確認：
   ```bash
   ls -la ~/Library/CloudStorage/GoogleDrive-*/マイドライブ/Archive/2026-08/
   # 環境により "My Drive" 表記の場合あり。Finderのサイドバーから辿ってもよい
   ```
3. 確認ポイント：
   - ファイルが**見える**だけでなく、サイズが実サイズである（0バイトや雲マークでない）
   - `file <ファイル名>` で中身の形式が判定できる（＝実体がある証拠）
4. ここまで通ったら、そのフルパスをSTATUS.mdに記録
   （B-01音声レーンのcronがこのパスを監視することになる）

## 完了チェックリスト
- [x] /Archive/2026-08/ が存在
- [x] ミラーリング or Archiveのオフライン固定が有効
- [x] Vault Cards/ が存在
- [x] スマホ投入→Macローカルパスで実体読み取り、が1件通った
- [x] 監視パスをMacローカルの環境変数に設定し、結果をSTATUS.mdに記録（実値は非公開）

## 注意
- iCloudのVault側は今回触らない（既存運用のまま）
- バックアップ（C-05）は未着手であることを忘れない。実データを貯め始めたら早めに
