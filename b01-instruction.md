# 実装指示書 B-01：音声レーン（Drive投入→ローカル文字起こし→索引カード自動生成）

## 背景（実装者へのコンテキスト)

「1つの置き場、複数のレンズ」の入れる側・第1レーン。iPhoneからGoogle Drive
`/Archive/YYYY-MM/` に投入された音声メモを、常時起動のMacBook Air（16GB）が検知し、
**ローカルで**文字起こしして、タイトル・要約・タグ付きの索引カードとして
Vault `Cards/` に自動保存する。人が書くフィールドはゼロ（索引カードv0.1準拠）。

- 前提: A-02完了（Driveミラーリング済みローカルパスと `Cards/` が存在すること）
- 完了定義: スマホで録音→放置→数分後に `Cards/` に読めるカードが生えており、
  `/find` でその内容が検索にヒットする
- 入口はDriveに一本化する。iCloud共有を第2の入口にはしない（箱を増やさない）

## 確定済みの設計判断（変更不可）

1. **文字起こしはローカル実行**。クラウドASRは使わない（医療系機微を外に出さない）
2. **エンジンはコマンド差し替え式**（A-01の `KH_CLAUDE_CMD` と同じ流儀）:
   - 文字起こし: `KH_ASR_CMD`（既定: whisper.cpp。医療用語対応OSSへ差し替え可能）
     契約: 引数に音声ファイルパス、stdoutにプレーンテキスト、失敗はexit非0
   - カード生成LLM: `KH_CLAUDE_CMD`（既定: `claude -p`。Ollama等ローカルLLMへ差し替え可能）
3. **原本不変**: `/Archive/` のファイルはリネーム・移動・削除しない。
   カードのfrontmatterに原本パスを記録してつなぐ
4. **タイトルは自動生成**: ファイル名でなく内容からLLMが命名（要約・タグと同一呼び出しで）
5. **全文文字起こしをカード本文に含める**: 要約だけでは検索網羅性が落ちる。
   ripgrepが「喋った単語そのもの」にヒットする状態を作る
6. 16GB Airの制約: ASRとLLMは**順次実行**（同時常駐させない）

## パイプライン仕様

```
[launchd/cron 数分毎] → 新着検知 → KH_ASR_CMD(文字起こし) → KH_CLAUDE_CMD(タイトル・要約・タグ) → Cards/保存 → 処理台帳に記録
```

### 1. 検知
- 監視対象: `$KH_ARCHIVE_PATH`（A-02で確立したローカル実パス。値はコミットしない）
- 対象拡張子: m4a / mp3 / wav / aac
- **同期中のファイルを掴まない**: 前回スキャンとサイズが一致するまで処理を保留
- 二重処理防止: 処理台帳（Vault外のstateファイル、例 `~/.kh_voice_processed`）に
  相対パス＋サイズを記録。台帳にある物はスキップ。失敗時は台帳に書かず次回リトライ

### 2. 文字起こし
- `KH_ASR_CMD <音声パス>` を引数配列で起動（シェル文字列連結禁止、A-01と同じガードレール）
- タイムアウト: 音声1分あたり60秒を上限目安に十分長く（バッチなので速度より完走）

### 3. カード生成
- LLMに文字起こし全文を渡し、JSON 1個を返させる:
  `{"title": "...", "summary": "2〜3文", "tags": ["...", "..."]}`
- プロンプト要件: (a)タイトルは内容ベース15字以内・ファイル名に使えない文字を含めない
  (b)要約は「何を考えた/決めた/調べたか」を優先 (c)創作・補完をしない
- LLM失敗時の縮退: `title=日付_時刻_音声メモ`、summary無しで**カード保存は必ず実行**
  （文字起こし結果を失わないことを最優先）

### 4. 保存
- パス: `Cards/YYYY-MM-DD_<title>.md`（衝突時は連番付与）
- 形式:

```markdown
---
type: voice
captured_at: 2026-08-01 08:12
source: Archive/2026-08/0801_0812_通勤メモ.m4a
summary: <2〜3文>
tags: [tag1, tag2]
---
## 要約
<summary再掲>

## 全文文字起こし
<ASR出力そのまま>
```

- `captured_at` はファイル名の `MMDD_HHmm` から復元し、無ければファイルのmtime

## 受け入れ条件（四関門）

1. ユニット: 偽ASR・偽LLM（fixtureスクリプト）で、検知→保存→台帳・縮退経路・
   同期中保留・二重処理防止が全てテストgreen
2. 結合: Mac実機で実音声1件がカードになる（whisper.cpp実行込み）
3. 実データ: 実際の通勤メモ級の録音でタイトル・要約が妥当、かつ
   `python3 -m knowledge_hub.find` で喋った内容の単語がヒットする
4. 本番: 1週間「録って放置」で運用しShojiが判定→STATUS.mdに記録

## 注意

- **C-05（バックアップ）をこのレーンの稼働開始前に必ず着手**。実データが貯まり始める
- レッドライン: 患者特定情報は録音時点で入れない。これは運用ルールであり
  本レーンに検知機構は実装しない（誤検知で本文を失うリスクの方が大きい）
- ローカルLLM移行: まず `claude -p` で品質基準を作り、Ollama（7〜14B・4bit量子化、
  日本語に強いモデル）に `KH_CLAUDE_CMD` を差し替えて比較。十分ならローカル固定
- launchdの実行間隔は5分程度から。Airの熱・電池は単発バッチなら問題にならない
